/**
 * Define and build String, Column, and numeric SQL expressions. 
 * Allows for math operations (add, subtract,etc) and string operations (concat)
 */
package com.bobman159.rundml.core.exprtypes;