/**
 * Defines the interfaces for SQL expression types (String, Column, and numeric) used in building SQL expressions. 
 * Allows for math operations (add, subtract,etc) and string operations (concat)
 */
package com.bobman159.rundml.core.sql.types;